import React from "react";
import styled from "styled-components";

const FeatureCard = ({ icon, content }) => {
  return <cardWrapper>this is card</cardWrapper>;
};

const cardWrapper = styled.div`
  display: flex;
`;

export default FeatureCard;
